<%- include('../partials/header.ejs') %>

<main class="page-content auth-page">
  <div class="auth-card">
    <!-- Login Form -->
    <div id="login-form" class="auth-form">
      <h1>Welcome back</h1>
      <p style="color: #666; margin-bottom: 1.5rem;">Sign in to your BagCartel account</p>
      
      <form action="/login" method="post">
        <div>
          <label for="email">Email Address</label>
          <input type="email" id="email" name="email" required placeholder="you@example.com" />
        </div>
        <div>
          <label for="password">Password</label>
          <input type="password" id="password" name="password" required placeholder="••••••••" />
        </div>
        <button type="submit" class="button button-primary" style="width: 100%; margin-top: 0.5rem;">Sign In</button>
      </form>

      <% if (googleEnabled) { %>
      <div class="oauth-section">
        <p>Or continue with</p>
        <a href="/auth/google" class="oauth-button">
            <img src="../assets/icons8-google-48.png" class="google-icon">
          Google
        </a>
      </div>
      <% } %>

      <div class="form-toggle" style="margin-top: 1.5rem; text-align: center;">
        <p style="margin: 0; color: #666;">Don't have an account? 
          <button type="button" onclick="toggleForms()" class="form-toggle-btn">Sign up</button>
        </p>
      </div>
    </div>

    <!-- Register Form -->
    <div id="register-form" class="auth-form" style="display: none;">
      <h1>Create account</h1>
      <p style="color: #666; margin-bottom: 1.5rem;">Join BagCartel and start shopping</p>
      
      <form action="/register" method="post">
        <div>
          <label for="register-email">Email Address</label>
          <input type="email" id="register-email" name="email" required placeholder="you@example.com" />
        </div>
        <div>
          <label for="register-password">Password</label>
          <input type="password" id="register-password" name="password" required placeholder="••••••••" />
        </div>
        <div>
          <label for="confirm-password">Confirm Password</label>
          <input type="password" id="confirm-password" name="confirm-password" required placeholder="••••••••" />
        </div>
        <button type="submit" class="button button-primary" style="width: 100%; margin-top: 0.5rem;">Create Account</button>
      </form>

      <div class="form-toggle" style="margin-top: 1.5rem; text-align: center;">
        <p style="margin: 0; color: #666;">Already have an account? 
          <button type="button" onclick="toggleForms()" class="form-toggle-btn">Sign in</button>
        </p>
      </div>
    </div>
  </div>
</main>

<%- include('../partials/footer.ejs') %>

<style>
  .form-toggle-btn {
    background: none;
    border: none;
    color: #667eea;
    font-weight: 700;
    cursor: pointer;
    text-decoration: underline;
    transition: color 0.3s ease;
  }

  .form-toggle-btn:hover {
    color: #764ba2;
  }

  .auth-form {
    animation: fadeIn 0.3s ease;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<script>
  function toggleForms() {
    const loginForm = document.getElementById('login-form');
    const registerForm = document.getElementById('register-form');
    
    loginForm.style.display = loginForm.style.display === 'none' ? 'block' : 'none';
    registerForm.style.display = registerForm.style.display === 'none' ? 'block' : 'none';
  }
</script>
